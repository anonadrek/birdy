# Visual companion / brainstorming workspace (lokalt arbetsmaterial)
.superpowers/

# Claude Code per-user-state
.claude/settings.local.json
.claude/cache/
**/.claude/settings.local.json

# Gradle / Kotlin Multiplatform
.gradle/
.kotlin/
build/
# Un-ignore Kotlin source packages named "build" (e.g. se.birdy.content.build)
!**/src/**/kotlin/**/build/
out/
local.properties
*.hprof

# IntelliJ / Android Studio
.idea/
*.iml
*.iws
*.ipr
captures/
.cxx/
.externalNativeBuild/

# Xcode (för iOS-modulen senare)
xcuserdata/
*.xcuserstate
DerivedData/
Pods/

# Build-artefakter
*.apk
*.apks
*.aab
*.aar
*.ipa
*.dSYM.zip
*.dSYM

# OS / editor-skräp
.DS_Store
Thumbs.db
*.swp
*~

tools/content-pipeline/.cache/
tools/content-pipeline/.env
tools/content-pipeline/sparse_content.yaml

# Signing keystores — never commit
keystore/
*.jks
*.keystore
/keystore.properties

# Local secrets — never commit (see gradle.properties.example)
gradle.properties

# BirdNET labels source file — large, reproducible, not committed
tools/birdnet_labels.txt

# xeno-canto API v3 key — never commit (per-user secret)
tools/ml-eval/.xc_key
tools/ml-eval/corpus_audio/

# Website (Astro landing page)
website/node_modules/
website/dist/
website/.astro/
website/playwright-report/
website/test-results/

# Git worktrees (local feature branches)
.worktrees/

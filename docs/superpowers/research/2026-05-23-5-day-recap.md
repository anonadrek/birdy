# 5-day recap — 2026-05-19 → 2026-05-23

**Sammanfattat:** 155 commits över 5 dagar. Vi tog projektet från `v0.9.0a-billing` (Billing v8 + launch-prep) till **`v1.0.0` taggad** + en publicerad marketing-website på `birdy.community` + en omfattande pre-release audit fullt åtgärdad. App:en är nu launch-redo — kvar är endast manuella Play Console-steg.

## Tags shippade i fönstret

| Tag | Datum | Innehåll |
|---|---|---|
| `v0.9.0b-audio` | 2026-05-21 | Plan 6b2 — Audio-ID via BirdNET-Lite |
| `v0.9.0c-premium-content` | 2026-05-23 | Plan 6b3 — PDF + season stats + 10 fält-märken |
| `v1.0.0` | 2026-05-23 | versionName 1.0.0, versionCode 112 |
| `v0.9.0c-listen-open` | 2026-05-23 | Post-v1.0 audio-UX refresh (tap-to-toggle) |

Commits/dag: 2026-05-20 = 3 · 2026-05-21 = 67 · 2026-05-22 = 47 · 2026-05-23 = 38.

---

## 2026-05-20 — Bridge mellan 6b1 och 6b2 (3 commits)

- `0ae9076` smoke-test signed AAB `v1.0.0-rc2` — release-build crash fixed (avslutar Plan 6b1).
- `8068297` + `9716024` design-spec + implementation-plan för Plan 6b2 (Audio-ID via BirdNET-Lite).

---

## 2026-05-21 — Audio-ID + marketing-website (67 commits)

Två stora parallella spår landade samma dag.

### Plan 6b2 — Audio-ID via BirdNET-Lite → `v0.9.0b-audio`

| Task | Vad |
|---|---|
| T1 | Bundle BirdNET 6K Global + label-mapping + validator |
| T2 | `:shared:ml` audio-foundation interfaces + label-mapper (`birdnet_lite_to_qid.json`) |
| T3 | TFLite audio-runner + lazy bootstrap (AtomicReference+CAS), FakeAudioClassifier i commonMain |
| T4 | `AndroidAudioRecorder` + `AudioPreprocessor` (int16→float32) + UNPROCESSED→VOICE_RECOGNITION graceful fallback |
| T5 | `AudioScanScreen` + permission flow + `WaveformBars` + 19 strängar; state-machine races fixade |
| T6 | DB schema v2→v3 (`audio_path` + `source`), `FileCleanupRequest`, real `WaveformRenderer` (Canvas PNG + MediaCodec Opus), `MatchResult.ScanSource`, source-aware `MatchView` marginalia (SV+EN) |
| T7 | 3-layer Premium gate för audio scan entry (senare rivet, se 6b3-spåret nedan) |
| T8 | ML-eval-scripts + BirdNET-Lite audio accuracy report, xeno-canto API v2→v3 migration |
| T9 | 10 canonical device-screenshots + ship-commit |

Fix på samma dag: `2ac6b37` la till `tensorflow-lite-select-tf-ops:2.16.1` (annars failar FlexRFFT node 29 "Failed to prepare").

### Plan W — Marketing-website (Astro 5 + Tailwind v4)

23 tasks i en enda dag, från noll till live landing-page.

- **T1–T7:** Astro 5 + Tailwind v4 scaffold, design tokens, self-hostade fonts (DM Serif Display, Caveat, Inter woff2 latin), i18n-infrastruktur (copy JSON + typed helper + parity check), asset sync-script, `Layout.astro` med meta/OG/hreflang/JSON-LD, UI primitives (brackets, ornament, eyebrow, wing-logo, wordmark).
- **T8:** `JournalHeadline`-parser för web (accent font-size 1.15em för visual parity med Compose).
- **T9–T11:** `PlayStoreBadge` + `DeviceFrame` + `FaqItem` primitives, `Nav.astro` (sticky + reduced-motion), `Hero.astro` (Section 1).
- **T12–T19:** Section 2–9 — `Loop.astro` (4 sub-cards Point/Match/Stamp/Browse), `Listen.astro`, `Inside.astro` (839/25/365 stat-trio), `Premium.astro` (free-first soft card), `Privacy.astro`, `Faq.astro`, `FinalCta.astro`, `Footer.astro`.
- **T20–T22:** EN (`/`) + SV (`/sv/`) landing-pages assembled, Playwright smoke tests + i18n parity gate, Astro build wired in `pages.yml` med `CNAME = birdy.app`.

Plus `877dbc9` — byte av placeholder-badges mot officiella Google Play PNGs.

### Launch-prep

- `2f4bab6` Claude Project knowledge file (för browser-Claude).
- `1869696` 6-agent Play Store audit findings (`docs/superpowers/research/2026-05-20-play-store-audit.md`).

---

## 2026-05-22 — Plan 6b3 grunden + brand refresh + /legal/ + license-pivot (47 commits)

### Plan 6b3 — Premium content (T1–T20)

Premium-tier feature-bygge:

- **T1–T2:** Scaffold `:shared:pdf` module + `JournalPdfRenderer` expect/actual + sealed result.
- **T3:** `BadgeRepository.unlockManualBadge` (idempotent) — Mutex skyddar boolean-returvärdet, inte SQL-racet.
- **T4:** `PremiumActivationListener` — manual unlock för `field_member` vid Premium-flip.
- **T5:** Real `JournalPdfRenderer` med title/stats/species/badges/colophon-sidor.
- **T6–T8:** `ExportJournalUseCase`, `AppGraph.journalExport` + share-launcher, `PremiumTeaserCard.onExport` CTA + Archive share-sheet integration.
- **T9–T12:** `SeasonStatsViewModel` (month/season/top-species/cumulative-aggregation), Canvas-baserade bar/line/donut-chart composables (0 deps), `SeasonStatsScreen` (JournalScaffold + LazyColumn chart sections), `AppRoute.SeasonStats` + Live/Locked stats-preview swap på Lifelist.
- **T13–T18:** 5 nya `BadgeRule`-varianter + `Badge.isPremium`-flag, `RecalculateBadgesUseCase` evaluerar 5 nya premium rules, `premium_badges.yaml v2` + `BadgeCatalogLoader.parsePremium` + Manual rule, 40 SV+EN strings + `BadgeStringMap`, `PremiumBadgesSection` (locked-row vs real 10-stamp grid), badge backfill re-runs vid Premium-flip.
- **T19:** Wire `PremiumActivationListener` + `effectivePremiumActive` på `AppGraph`.
- **T20:** Bump versionName `1.0.0-rc3` + Billing v8 ProGuard keeps + Java21 align.
- **T25:** Internal Testing hand-off runbook (`docs/superpowers/runbooks/2026-05-22-v1.0.0-internal-testing.md`).

### BirdNET-licensbeslut — Option A

`79ad50b` + `f8eaab7` — rev audio-nav-gate efter att BirdNET-Lite-modellen identifierats som CC BY-NC-SA 4.0 (NonCommercial). **Audio-ID är gratis i v1.0.** Premium-tier revenue står på Plan 6b3-features (PDF/stats/badges) vi byggt själva.

`eacf22c` — `PREMIUM_OPEN_FOR_LAUNCH=true` ger alla användare LIFETIME-premium under closed testing + initial production (flippas till `false` när Billing v8 IPC är runtime-verifierad).

### Brand refresh

- `0c76113` launcher icon refresh + custom Compose splash med wordmark.
- `58a47fb` website hero — bytte brand-foot wordmark mot fågel-app-ikon med fade-up.
- `7a6eb8b` Glimpse carousel-sektion på website med 6 Play Store-skärmdumpar.
- `76f861a` transparenta multi-size favicons (.ico + apple-touch).
- `0d1b93e` Settings — webbplats-länk i OM BIRDY + Language-ikonbyte.
- `92c40b3` Settings — privacy/terms-länkar migrerade till `birdy.community/legal/`.

### Website /legal/-routes (T3–T8)

`marked` dep + markdown-helper, `LegalLayout` + prose styles, `/legal/` index-page, dynamic `/legal/[slug]/`-page för 3 docs (privacy/terms/data-safety), footer-länkar synkade för båda locales, Playwright smoke tests med console-error capture.

### Plan-listen spec

`956abdc` + `cf33eca` — spec + plan för open-ended recording (matchar website-mock).

---

## 2026-05-23 — Ship v1.0 + Listen UX refresh + pre-release audit (38 commits)

### Plan 6b3 ship — `v0.9.0c-premium-content` + `v1.0.0`

- **T21:** Device-verify på SM-S918B + 10 screenshots. Bug-fixes: Archive export-CTA + locale-aware species (`12f1ed2`).
- **T22a/T22b (`4fc9664`):** WebP-migration av 2062 species-bilder (JPG→WebP q75) + nytt `:asset-pack`-module (install-time, deliveryType=install-time, dist:fusing true). `targetImages = project(":asset-pack").file(...)`, ny expect/actual `speciesImageUri()` ersätter `Res.getUri()` i HeroImage/Archive/SpeciesProfile/MatchView/DisambigView. **Base APK download: ~420 MB → 136 MB** ✅ (under Play Store 150 MB cap). Asset-pack split: 319 MB install-time.
- **T24 (`595a48a`):** Bump versionName `1.0.0-rc3 → 1.0.0` (versionCode 112).
- **Tagged `v0.9.0c-premium-content` + `v1.0.0`.**

### Plan Listen — Open-ended recording → `v0.9.0c-listen-open`

11 tasks som byggde om audio-flowet från "håll 3s" till tap-to-toggle med sliding-window classify.

| Task | Vad |
|---|---|
| T1 | `Top1` + `bestSoFar` field på `AudioScanState.Recording`; pcmOffset/pcmEnd half-open range |
| T2 | Streaming `AudioRecorderApi` + `FakeStreamingRecorder` test-double; thread-safety + idempotency contract |
| T3 | Sliding-window classify + `bestSoFar` + auto-stop i ViewModel; atomic finalize, @Volatile, cancel cleanup |
| T4 | `AndroidAudioRecorder` streaming + cap-driven stop |
| T5 | `WaveformBars` 48 staplar AccentCopper 80dp (matchar website) + idle-pulse + boostad RMS |
| T6 | `RecordingMicButton` 64dp copper — Idle/Recording/Disabled/Analyzing states |
| T7 | Strings update — riv "3s"-language, ny `cta_idle` + `cta_recording` |
| T8 | `AudioScanScreen` + Host — tap-to-toggle + `RecordingMicButton` + count-up timer |
| T10 | 5 device-screenshots på SM-S918B |
| T11 | Website — sync copy + riv hardcoded "hold 3s" i `Listen.astro` |

Plus `2b3085d` — fix(audio) BirdNET-Lite flat sigmoid så confidences landar i [0, 1].

### Pre-release audit — 7 parallella agenter

`7ea5de5` — fältrevision i större skala än 2026-05-20 (7 vs 6 agenter). Rapporter i `docs/superpowers/research/2026-05-23-prerelease-audit/01-07-*.md`, master-checklista i `00-MASTER-checklist.md`.

Fixar pushade i 8 commits:

| Commit | Audit-ID | Vad |
|---|---|---|
| `7e6957c` | B1 | bumpa navigation-compose alpha10 → alpha13 |
| `cc1530d` | B2 | migrera listing-URLer github.io → birdy.community |
| `05b473b` | B3 | v1.0.0-narrativ för store-listings |
| `43bec7b` | — | runbook — website opt-in-länk-byte under Internal Testing |
| `e670b09` | B4 + H2-H5 + M3 + M5 | build-config + Android 13 Language Picker + tester-instr |
| `6cc5d4f` | H8-H11 + M6-M9 | null-safety + safe casts + DB-guards |
| `1fc7245` | — | markera B4 + alla H + M klara i master-checklistan |

**Resultat:** Alla kod- och dokument-fixar landade.

- ✅ 4 BLOCKERS
- ✅ 11 HIGH (H1 deferred → manuell Play Console; H6/H7/H9 var fel-premiss)
- ✅ 9 MEDIUM (M1/M2 deferred → manuell Play Console; M4 var fel-premiss)
- 🟢 6 LOW noterade, ignorerade

### Roadmap-docs

- `5e83c2e` geografisk expansion som långsiktig huvudtrack (v2/v3).
- `6b85851` iOS-launch bakas in i v2 ("Asien + EU" + App Store).

### Övriga städ-fixar

`587a492` CLAUDE.md sync till v1.0.0, `1cb54f4` ktlint MissingPermission + NewApi i shared:ml + Billing, `e0d6f58` plan-docs + launch research + splash/launcher iteration screenshots, `6279098` ignorera `*.apks` + nested `.claude` config, `d5e55d5` ignorera `.worktrees/`.

---

## Var står vi nu?

**App:** `v1.0.0` taggad. Branch `main` är clean och pushad. Inga fler kod-ändringar behövs innan AAB-build.

**Website:** Live på `https://birdy.community` via Vercel. `/legal/{privacy,terms,data-safety}/`-routes på plats. GitHub Pages-deployen är avskaffad.

**Kvar för Play Store-launch — endast manuella Play Console-steg:**

| ID | Vad | När |
|---|---|---|
| H1 | `PLAY_LICENSE_KEY` hämtas från Play Console | Under upload-flowet |
| M1 | Verifiera SKU 199/499 SEK in-app products | Under upload-flowet |
| M2 | Fyll i Data Safety form | Under upload-flowet |

Runbook: `docs/superpowers/runbooks/2026-05-22-v1.0.0-internal-testing.md`.

**Path till launch:** Internal Testing (idag) → Closed Testing (14 dagars klocka) → Production.

**Deferred post-launch:**

1. URL-migration i `SettingsScreen.kt` (rad ~128–129) från `anonadrek.github.io/birdy` till `birdy.community/legal/`.
2. GitHub Pages teardown (repo Settings → Pages → None).
3. Email-migration från `albin@abrahamssons.se` bridge till `feedback@birdy.community`.
4. Billing v8 IPC runtime-verify (kräver Play Console app entry + in-app products + license testers). När verifierad: flippa `PREMIUM_OPEN_FOR_LAUNCH=false` och bumpa versionCode.
5. Audio accuracy eval (kräver xeno-canto API v3 key).
6. AB-flytt via Account Transfer när AB är registrerat.
7. SV legal-översättningar om Sverige-trafik växer.
8. Plan 6a T8/T9 device-screenshots (`08-match-with-inline-note` + `09-disambig-save-as-unknown`).

**Roadmap post-v1.0:**

- **v2:** iOS-launch (App Store) + content-expansion till delar av Asien.
- **v3:** Globala arter, full content-skalning + språkstöd.
- Parallella feature-spår (inte version-bundna): "Karta & moln" (konton + sync + push), Community (delning/moderering), Quiz/utbildningsläge, offline-läge.
